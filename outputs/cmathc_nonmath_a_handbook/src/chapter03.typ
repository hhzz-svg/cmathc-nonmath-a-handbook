= 一元微分、中值定理与应用

#import "settings.typ": *

#chapter_intro[
  本章围绕导数的三种角色展开：计算局部变化、连接两个点的整体信息、构造证明所需的辅助函数。重点不在公式堆积，而在定理条件与目标式之间的匹配。
]

== 方法框架：导数如何提供整体信息

#method_card(
  [中值定理链],
  [结论含有差商、两点函数值或存在某个 $xi$],
  [先核对闭区间连续、开区间可导，再选择 Rolle、Lagrange 或 Cauchy 形式],
  [定理给出存在性，不自动给出同一个 $xi$ 可用于多个等式。],
)

#method_card(
  [单调与凸性],
  [题目要求根的个数、最值、唯一性或不等式],
  [把目标转成 $f' > 0$、$f'' > 0$ 或辅助函数的单调性],
  [临界点只提供候选；分段点、端点和定义域边界必须单独比较。],
)

#method_card(
  [参数方程与高阶导数],
  [曲线由 $x(t),y(t)$ 给出，或出现重复求导],
  [使用 $d y/d x=(d y/d t)/(d x/d t)$，高阶导数逐层保留分母因子],
  [需要注明 $d x/d t != 0$ 的区间；分段函数的拼接点不能直接套公式。],
)

== 精讲例题

#worked_example(
  "W03-01",
  "用 Cauchy 中值定理比较增长率",
  "中值定理、极限",
  "中",
  "10 min",
  "原创",
  [
    *题目：* 证明当 $x>0$ 时，$ln(1+x) < x$。

    *构造：* 在 $[0,x]$ 上取 $f(t)=ln(1+t)$、$g(t)=t$。两函数连续、开区间可导，且 $g'(t)=1$。Cauchy 中值定理给出某 $xi in (0,x)$ 使 $ln(1+x)=x/(1+xi)$。

    *结论：* 因 $xi>0$，有 $1+xi>1$，故 $ln(1+x)<x$。这个证明比直接调用“对数函数凹”更适合需要展示定理条件的题目。
  ],
) <W03-01>

#worked_example(
  "W03-02",
  "由辅助函数证明不等式",
  "Taylor、单调性",
  "中高",
  "12 min",
  "原创",
  [
    *题目：* 证明 $e^x >= 1+x$ 对所有实数 $x$ 成立。

    *观察：* 目标差值 $e^x-1-x$ 的导数为 $e^x-1$，仍需判断符号。设 $F(x)=e^x-1-x$，则 $F'(x)=e^x-1$，而 $F'(x)$ 在 $x=0$ 处变号，$F$ 在 0 处取全局最小。

    *解：* 也可用 Taylor 公式：$e^x=1+x+e^xi x^2/2$，其中 $xi$ 位于 0 与 $x$ 之间，余项非负。因此结论成立，且等号仅在 $x=0$。

    *选择：* 需要等号条件时，Taylor 余项一次完成；需要推广到一般凸函数时，辅助函数路径更易迁移。
  ],
) <W03-02>

#worked_example(
  "W03-03",
  "参数方程的曲率",
  "参数微分、曲率",
  "中高",
  "12 min",
  "原创",
  [
    *题目：* 曲线 $x=a(t-sin t), y=a(1-cos t)$ 中 $a>0$。求 $t=pi$ 处的曲率。

    *计算：* $x'=a(1-cos t), y'=a sin t$；$x''=a sin t,y''=a cos t$。参数曲线曲率为 $kappa=|x'y''-y'x''|/(x'^2+y'^2)^(3/2)$。

    在 $t=pi$ 处，$x'=2a,y'=0,x''=0,y''=-a$，故 $kappa=2a^2/(4a^2)^(3/2)=1/(4a)$。

    *边界：* 这里速度不为零，公式可用；若 $x'=y'=0$，必须换参数或单独讨论奇点。
  ],
) <W03-03>

#worked_example(
  "W03-04",
  "含参数函数的极值分类",
  "参数讨论、二阶导数",
  "高",
  "15 min",
  "原创",
  [
    *题目：* 讨论 $f_a(x)=x^3-3 a x$ 的极值，其中 $a>0$。

    *解：* $f_a'(x)=3(x^2-a)$，临界点为 $x=+-sqrt(a)$。在 $x=-sqrt(a)$ 处导数由正变负，取极大；在 $x=sqrt(a)$ 处由负变正，取极小。

    函数值为 $f_a(-sqrt(a))=2a^(3/2)$，$f_a(sqrt(a))=-2a^(3/2)$。参数只改变位置和幅度，不改变“一个极大、一个极小”的结构。

    *易错：* 只解出临界点而不做符号表，无法完成极值分类。
  ],
) <W03-04>

#worked_example(
  "W03-05",
  "Taylor 余项估计",
  "Taylor、误差界",
  "高",
  "14 min",
  "原创",
  [
    *题目：* 证明当 $x>0$ 时，$0 < x-ln(1+x) < x^2/2$。

    *解：* Taylor 公式给 $ln(1+x)=x-x^2/(2(1+xi)^2)$，其中 $xi in (0,x)$。因此差值为 $x^2/(2(1+xi)^2)>0$，又因 $1+xi>1$，得到差值小于 $x^2/2$。

    *关键：* 余项写成拉格朗日型时，$xi$ 的区间必须与题目定义域一致；不能直接把余项替换成一个没有来源的“约等于”。
  ],
) <W03-05>

#worked_example(
  "W03-06",
  "全局最值的边界比较",
  "闭区间最值、分段",
  "中",
  "10 min",
  "原创",
  [
    *题目：* 求 $f(x)=x+1/x$ 在 $[1/2,3]$ 上的最大值与最小值。

    *解：* $f'(x)=1-1/x^2$，临界点 $x=1$。比较 $f(1/2)=5/2$、$f(1)=2$、$f(3)=10/3$，所以最小值为 2，最大值为 $10/3$。

    *提醒：* 闭区间连续函数的全局最值来自“内部临界点 + 全部端点”，只做一阶导数不能跳过端点。
  ],
) <W03-06>

== 短例与章末训练

#quick_example(
  "Q03-01",
  "Rolle 的零点数",
  [若连续可导函数在两端函数值相等，至少存在一点导数为零；若要推出零点唯一，还需单调性。],
) <Q03-01>
#quick_example(
  "Q03-02",
  "凹凸性识别",
  [若 $f''>0$，函数图像向上凸，切线在图像下方；不等式方向要结合区间而不是只看一张草图。],
) <Q03-02>
#quick_example(
  "Q03-03",
  "反函数导数",
  [$(f^(-1))'(y)=1/f'(x)$，其中 $y=f(x)$ 且 $f'(x)!=0$；先写变量对应关系再代点。],
) <Q03-03>
#quick_example(
  "Q03-04",
  "渐近线",
  [水平渐近线看 $x -> +-oo$ 的极限，垂直渐近线看定义域端点或不可定义点的无穷极限，不能混用。],
) <Q03-04>

#exercise_set([X03-01--X03-04], [
  X03-01 证明 $sin x < x$ 对 $x>0$ 成立，并说明等号何时出现。

  X03-02 讨论 $x^4-2 a x^2$ 的极值结构，参数取 $a>0$。

  X03-03 求摆线在给定参数点的切线斜率，并检查速度是否为零。

  X03-04 求 $x^2+1/x$ 在 $[1,4]$ 上的最值，写出端点比较。
])
